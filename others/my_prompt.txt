# append to .bashrc

function __prompt_command() {
    local EXIT="$?"
    [[ $EXIT -eq 0 ]] || echo -n -e "\e[31m${EXIT}\e[0m "
}

if [ -z "$BYOBU_BACKEND" ]; then
    export PROMPT_COMMAND=__prompt_command  # Func to gen PS1 after CMDs
else
    unset PROMPT_COMMAND
fi
